{ "homepage": "https://hadhoudda.github.io/Portfolio",
"name": "portfolio",
"lockfileVersion": 3,
"requires": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
